# Build outputs
build/
app/build/
*/build/
.gradle/
.gradle-cache/
*.apk
*.aab

# Android Studio / IntelliJ
.idea/
*.iml
local.properties
captures/

# Nix
result
result-*

# Emulator state
.android/

# Keystore (release signing — never commit)
*.jks
*.keystore
keystore.properties

# AGPS token — see docs/AGPS_TOKEN.md. Holds a u-blox AssistNow
# developer token; baked into BuildConfig.AGPS_TOKEN at build time.
# Tracked sibling `agps.properties.example` documents the format.
app/agps.properties

# Misc
.DS_Store
*.log
tmp/
.claude/
