<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- BLE: Android 12+ permission split. `neverForLocation` skips the
         location-prompt for scans that don't infer position. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="s" />
    <!-- Legacy BLE permissions for pre-Android-12 devices (minSdk 26). -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ligpsport"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Ligpsport">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.share.ShareImportActivity"
            android:exported="true"
            android:label="@string/upload_to_igpsport"
            android:theme="@style/Theme.Ligpsport">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/gpx+xml" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="text/xml" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.UploadForegroundService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- adb test hook: the single receiver fan-out for the
             headless harness. Exported so `adb shell am broadcast`
             can reach it; the action strings are app-private so no
             other app routes here. Receiver itself is lightweight —
             long-running BLE work is dispatched onto
             UploadForegroundService. -->
        <receiver
            android:name=".cli.AdbCliReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="de.syntaxfehler.ligpsport.action.PAIR" />
                <action android:name="de.syntaxfehler.ligpsport.action.UNPAIR" />
                <action android:name="de.syntaxfehler.ligpsport.action.UPLOAD" />
                <action android:name="de.syntaxfehler.ligpsport.action.PLAN_AND_UPLOAD" />
                <action android:name="de.syntaxfehler.ligpsport.action.DELETE_ROUTE" />
                <action android:name="de.syntaxfehler.ligpsport.action.DELETE_ROUTE_BY_ID" />
                <action android:name="de.syntaxfehler.ligpsport.action.LIST_ROUTES" />
                <action android:name="de.syntaxfehler.ligpsport.action.DELETE_ALL_ROUTES" />
                <action android:name="de.syntaxfehler.ligpsport.action.NAV_STATUS" />
                <action android:name="de.syntaxfehler.ligpsport.action.SET_ROUTER" />
                <action android:name="de.syntaxfehler.ligpsport.action.LIST_ROUTERS" />
                <action android:name="de.syntaxfehler.ligpsport.action.MOCK_LOCATION" />
                <action android:name="de.syntaxfehler.ligpsport.action.STATUS" />
                <action android:name="de.syntaxfehler.ligpsport.action.SEND_AGPS" />
                <action android:name="de.syntaxfehler.ligpsport.action.SEND_LOCATION" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
